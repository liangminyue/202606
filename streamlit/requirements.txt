# 部署到 Streamlit Cloud 的依赖清单
# 关键：scikit-learn 必须锁定为 1.3.2，因为模型(.pkl)是用该版本序列化的；
# 若安装更新版本（如 1.8/1.9），会因内部模块/属性变更导致“加载模型失败”。
streamlit==1.28.0
pandas==2.3.3
numpy==1.26.4
joblib==1.3.0
plotly==5.17.0
shap==0.50.0
scikit-learn==1.3.2
xgboost==2.0.0
