# Streamlit Cloud 依赖清单（位于应用入口 streamlit_app.py 同目录 streamlit/，Cloud 会读取该目录下的 requirements.txt）
# 关键：scikit-learn 锁定 1.3.2，与模型(.pkl)序列化版本严格一致，否则上线后报“加载模型失败”。
streamlit==1.62.0
pandas==2.3.3
numpy==1.26.4
joblib==1.3.0
plotly==5.17.0
shap==0.50.0
scikit-learn==1.3.2
xgboost==2.0.0